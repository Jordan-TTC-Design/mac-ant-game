# 哥布林營地 (GoblinCamp) — Mac 桌面小遊戲計畫

## Context
想做一個上班摸魚用的小樂趣：螢幕上先由蟻后選位置築巢，之後每 10 秒冒出一隻小螞蟻，在整個桌面上走動，放著不管就越來越多。
決定做成 **Mac 原生 menu bar App**（螞蟻要爬在所有 App 之上，瀏覽器外掛做不到）。進度可存檔，也可在選單中關閉存檔。

環境：從空資料夾開始；只有 Command Line Tools（Swift 6.1.2），沒有完整 Xcode → 用 **Swift Package Manager + build.sh 手動包成 .app**，不依賴 Xcode 專案。

## 設計

**呈現方式**
- App 為 menu bar 常駐（`LSUIElement=true`，無 Dock 圖示），選單項目：暫停/繼續、存檔開關、重置蟻窩、（顯示螞蟻數）、結束。
- 每個 `NSScreen` 一個無邊框透明 `NSWindow`：`ignoresMouseEvents = true`（滑鼠穿透，不干擾工作）、`level = .statusBar`、`collectionBehavior = [.canJoinAllSpaces, .stationary, .fullScreenAuxiliary]`。
- 螞蟻用全域座標；每個視窗的 `AntView.draw` 以自己的 origin 偏移繪製，多螢幕自然銜接。

**遊戲流程**
1. 首次啟動（或重置後）進入「選巢位」：覆蓋視窗暫時 `ignoresMouseEvents = false`、游標改十字，顯示提示「點一下選擇蟻窩位置」。
2. 點擊 → 蟻后從螢幕邊走到該點，停下、挖洞（畫一個小土堆/洞口），視窗恢復滑鼠穿透。
3. 之後每 10 秒從巢口生出一隻螞蟻；蟻后留在巢旁。
4. 螞蟻行為：隨機漫步（heading 每幀小幅隨機轉向、偶爾停頓）、速度約 15–40 px/s、碰到螢幕邊緣反彈；小小的（約 4–6 px 身體 + 腿的擺動），黑/深褐色。
5. 上限預設 500 隻（防止長時間掛著吃 CPU）。

**存檔**
- 選單勾選「儲存進度」（預設開）。存 `~/Library/Application Support/GoblinCamp/state.json`：巢座標、螞蟻數、上次生成時間。
- 重開後：還原巢與螞蟻數（螞蟻位置重新散布在巢附近）；不做離線補生。關閉存檔則每次都重新選巢。

**效能**
- 單一 `Timer`（30fps）驅動更新，每個視窗一個 view，在 `draw(_:)` 裡用 Core Graphics 一次畫完所有螞蟻。500 隻量級足夠。

## 檔案結構（全部新建）
```
ant/
├── Package.swift
├── build.sh                      # swift build -c release → 組 GoblinCamp.app + Info.plist + ad-hoc codesign
├── Resources/Info.plist          # LSUIElement, bundle id
└── Sources/GoblinCamp/
    ├── main.swift                # NSApplication 啟動
    ├── AppDelegate.swift         # menu bar 選單、建立/銷毀 overlay、螢幕變更監聽
    ├── Colony.swift              # 遊戲狀態：階段(選巢/運行)、巢位置、蟻后、螞蟻陣列、10 秒生成計時
    ├── Ant.swift                 # 單隻螞蟻：漫步、回巢休息、發現食物、覓食、搬運（狀態機）
    ├── Food.swift                # 食物種類、食物、螞蟻看得到的世界（AntWorld）
    ├── Queen.swift               # 蟻后狀態機（20 種動作、對滑鼠與新生螞蟻的反應）
    ├── Settings.swift            # 選單設定（生成速度、上限、大小、速度、顏色…）存 UserDefaults
    ├── NestImageStore.swift      # 自訂蟻窩圖片
    ├── OverlayWindow.swift       # 透明覆蓋視窗（穿透 / 選巢模式切換）
    ├── AntView.swift             # 繪製螞蟻、蟻后、巢、提示文字；選巢時處理 mouseDown
    └── Persistence.swift         # state.json 讀寫
```
偵錯用環境變數 `CAMP_SPAWN_INTERVAL`（預設 10）方便快速測試。

## 執行方式（依使用者回饋調整）
- 核准後**第一步**：把本計畫複製成專案內的 `PLAN.md`，並在每個階段完成後勾選進度。
- **逐步開發**：每完成下面一個階段就實際建置、執行驗證，簡短回報結果後再進下一階段，不一次寫完。

## 分階段實作
- [x] 1. Package.swift + build.sh + 最小 App：menu bar 圖示 + 全螢幕透明穿透視窗，可正常結束。
- [x] 2. 選巢流程：十字游標點擊 → 記錄巢位置。（點擊選巢待人工確認）
- [x] 3. 螞蟻生成與漫步、蟻后、巢的繪製。
- [x] 4. 存檔/還原 + 選單開關 + 重置。（含選單設定：生成速度、上限、大小、移動速度、顏色、暫停）
- [x] 4b. 蟻窩自訂圖片（選單「蟻窩圖案」，圖片複製到 Application Support/GoblinCamp/nest.png）
- [x] 4c. 蟻后行為：20 個動作全部完成（含對滑鼠的反應、點蟻窩探頭、里程碑跳舞），見 [QUEEN_BEHAVIORS.md](QUEEN_BEHAVIORS.md)
- [x] 4d. 蟻后登場改為從洞口慢慢爬出來（原本從螢幕左邊走進來）
- [x] 4e. 預設蟻窩改成會成長的小土堆（見開發紀錄）
- [x] 4f. 編輯模式（拖曳移動蟻窩）與選位置時按 Esc 取消（見開發紀錄）
- [ ] 5b. 角色系統與哥布林（點陣風、四方向、16×16）：分三步——① 點陣角色引擎＋內建哥布林＋螞蟻改成可選角色（**已完成**）② 行為與用詞換成哥布林版（營地改洞穴、專用影格；選單用詞已隨角色切換）③ 角色編輯器（見「角色系統與哥布林」）
- [x] 5c. 改名為「哥布林營地 GoblinCamp」、選單列改用自畫的哥布林頭像、新增 App 圖示（見「改名、圖示與搬家」）
- [x] 4g. 食物（水滴、蜂蜜）與覓食行為：碰到才發現、回巢通知、圍住、搬運，以及螞蟻回巢休息（見「覓食行為」）
- [x] 5. 多螢幕熱插拔、效能優化、500 隻壓力測試（暫停已在階段 4 做完）。

## 驗證
- `./build.sh` 成功產出 `GoblinCamp.app`；首次執行需右鍵 → 開啟（ad-hoc 簽署，非 Apple 認證）。
- `CAMP_SPAWN_INTERVAL=1 open GoblinCamp.app`：確認選巢提示 → 點擊 → 每秒出現一隻螞蟻並漫步；切到 Chrome/Finder 時螞蟻仍在最上層，且點擊、輸入不受影響（滑鼠穿透）。
- 存檔：累積數隻後結束再開，確認巢與數量還原；關閉「儲存進度」後重開，回到選巢畫面。
- 若有外接螢幕，確認螞蟻可在螢幕間走動；用 `screencapture` 截圖檢查螞蟻大小與外觀。
- 放 500 隻時用 Activity Monitor 檢查 CPU 占用（目標 < 5%）。

## 已知限制
- 沒有 Apple Developer 憑證：分享給同事需右鍵開啟或 `xattr -d com.apple.quarantine`。
- 公司若以 MDM 擋未簽署 App，則此方案無法安裝。

## 開發紀錄
- 階段 1～4 完成；選單設定、蟻窩自訂圖片、蟻后行為（6 個）已加入。
- 設定不是 `.env`：Mac App 不會讀 `.env`，設定一律用選單，存在 UserDefaults（網域 `dev.goblincamp.game`）。`CAMP_SPAWN_INTERVAL`、`CAMP_AUTO_NEST`、`CAMP_DEBUG` 是開發測試用的環境變數，需直接執行 `GoblinCamp.app/Contents/MacOS/GoblinCamp` 才會帶入。
- 螞蟻外觀之後想擴充成更多樣子：目前 `AntView.addAnt` 集中定義螞蟻的形狀，`AntColorTheme` 管顏色，擴充時從這兩處著手。
- 階段 5 效能（全螢幕透明視窗，Retina 螢幕，CPU 為單一程序的即時值，量測有 ±3% 的雜訊）：

  | 螞蟻數 | 逐隻繪製（原本） | 現在（批次路徑 + 自適應 fps） |
  |---|---|---|
  | ~50 | 約 7% | 約 8% |
  | ~150 | 約 11% | 約 8% |
  | 500 | 約 25% | 約 13% |

  - 有效：所有螞蟻的身體、腿合併成兩條 `CGPath` 一次畫完（`AntView.addAnt` / `paint`）；螞蟻超過 100 隻時降到 20fps（`AppDelegate.crowdedAnts`）。
  - 無效、已退回：只重畫螞蟻移動過的格子（局部 `setNeedsDisplay`）——每個數量都更慢（56 隻 24% vs 7%），AppKit 的分段繪製開銷比整個重畫還大；`wantsLayer` 圖層繪製——沒有差別。
  - 目標「< 5%」沒有達成：主要成本是每幀整個螢幕重畫（2940×1912），這是這個做法的下限。要再降需換成 Core Animation 每隻一個 layer 之類的 GPU 合成做法，工程較大，暫不做。500 隻要連續掛機約 80 分鐘（10 秒一隻）才會出現。
- 多螢幕熱插拔：監聽 `didChangeScreenParametersNotification` 重建覆蓋視窗；`Colony.updateWalkable` 會把跑出螢幕外的螞蟻搬到最近的螢幕，巢若在被拔掉的螢幕上則移到剩下螢幕的最近邊緣（蟻后重新就位）。純邏輯模擬通過；未實際插拔螢幕測試。
- 開發用環境變數（需直接執行 `GoblinCamp.app/Contents/MacOS/GoblinCamp`）：`CAMP_SPAWN_INTERVAL`（生成間隔秒數）、`CAMP_AUTO_NEST`（啟動時自動在主螢幕中央築巢）、`CAMP_DEBUG`（log 每次生成）、`CAMP_NO_SAVE`（不讀寫存檔，測試用）、`CAMP_SNAPSHOT=/path/prefix` + `CAMP_SNAPSHOT_AFTER=秒`（App 截自己的視窗存成 PNG，不需螢幕錄製權限）。UserDefaults 的設定也能用命令列覆蓋，例如 `-maxAnts 500 -colorTheme black`，不會寫進設定。
- 已確認：螞蟻外觀（自我截圖）；選單設定與蟻窩圖片可用（UserDefaults 裡有使用者調過的上限、顏色，截圖中也有自訂蟻窩圖片）。
- 尚未人工確認：點擊選巢、蟻后部分動作的實際畫面（跳舞、轉圈、張望、修補、好奇/被嚇到）、點蟻窩探頭（全域滑鼠監聽）、實際插拔螢幕。
- 蟻后登場：從洞口慢慢爬出來（`Queen.emerging` + 以洞口為界的裁切），取代原本從螢幕邊緣走進來。
- 預設蟻窩（`AntView.drawNest` / `drawMound`）：小土堆有陰影、漸層、土粒與洞口；寬度 = 18 + 16 × (1 − e^(−螞蟻數/120))，即 0 隻 18pt、約 100 隻 27pt、500 隻約 34pt，之後幾乎不再變大；螞蟻 ≥ 60 隻多一個小土堆、≥ 200 隻再多一個；土粒用固定亂數種子（依巢位置），不會逐幀閃動。有自訂圖片時改畫圖片。
- 蟻后 20 動作的實作中發現並修掉「目標落在轉彎半徑內 → 原地繞圈走不到」的 bug（詳見 QUEEN_BEHAVIORS.md「踩過的坑」）。
- 測試用環境變數另有：`CAMP_QUEEN_FORCE=動作名`（強制蟻后進入某動作）、`CAMP_NO_NEST_IMAGE`（忽略自訂蟻窩圖片）。完整列表見 README。
- 想法（尚未決定）：螞蟻改成點陣（pixel art）風格。傾向做成選單可切換的「外觀風格」而不是直接取代；需要把螞蟻繪製抽成可替換的「外觀」，點陣版要 8 方向 × 數個走路影格的小圖（可用程式內的字元格定義，不需外部素材）、用最近鄰縮放，並把氣泡、表情符號也換成點陣版才不突兀。建議先在同一畫面做預覽比較再決定。
- 編輯模式與 Esc 取消：`Colony.Phase` 新增 `idle`（沒有蟻窩、也不在選位置；第一次啟動按 Esc 後的狀態）與 `editing`（覆蓋視窗攔截滑鼠，可拖曳蟻窩）。`beginPicking` 在已有蟻窩時**不動**原本的蟻窩，只有 `placeNest`（真的點下新位置）才清空螞蟻，所以 Esc 可以無損回到原狀；選位置期間原蟻窩凍結但仍畫出來。拖曳用 `window.convertPoint(toScreen:)` 取螢幕座標（不是即時滑鼠位置），拖到別的螢幕也正確，也讓合成事件能測試；`moveNest` 會把超出螢幕的位置夾回最近的螢幕、蟻后重新就位。Esc / Return 用 `NSEvent.addLocalMonitorForEvents`，不需要任何權限。選單項目的啟用狀態在 `menuNeedsUpdate` 依階段設定（`autoenablesItems = false`）。存檔規則改為「有蟻窩就存、沒有就清」，避免選位置期間清掉原存檔。
- 驗證：`Colony` 階段轉換單獨編譯的邏輯測試 25 項全過；App 內用合成事件（`CAMP_TEST_ESC`、`CAMP_TEST_EDIT`）確認 Esc 取消 → `idle`、拖曳 (+300, −150) 後蟻窩剛好移動、螞蟻不受影響、Esc 結束編輯；自我截圖確認選位置提示與編輯模式畫面。**尚未用真實滑鼠鍵盤操作確認**（實機游標樣式：十字、張開/握住的手；多螢幕拖曳）。

## 覓食行為（食物、通知同伴、搬運、回巢休息）

**設計目標**：像真的螞蟻——不刻意找食物，碰到才發現；發現的回巢通知；同伴出來一起圍住，慢慢搬回家；同時螞蟻有時會回巢休息。

**放食物**：選單「放食物 ▸ 水滴 / 蜂蜜」→ `Colony.Phase.placingFood`（覆蓋視窗攔截滑鼠，頂端提示、十字游標，Esc 取消；期間螞蟻照常走動）→ 點一下放下。上限 6 份（`Colony.maxFoods`，超過取代最舊的）。水滴 16 份、蜂蜜 24 份（`FoodKind.initialAmount`），每被叼走一份就縮小（半徑最小 40%），搬完消失。食物大小隨「螞蟻大小」設定稍微放大（`Colony.foodScale`）。

**螞蟻狀態機**（`Ant.Mode`，`Ant.update(dt:world:)` 回傳事件由 `Colony.handle` 處理）：

```
wandering ──走到食物 9px 內──▶ (食物還沒人通報) carryingNews ─回巢─▶ inNest(2~4s) ─▶ foraging   [事件 newsDelivered → 招募]
    │                          (已有人通報/發現)  foraging ─▶ feeding(2~4s) ─▶ hauling ─回巢(速度×0.7)─▶ inNest ─┐
    │                                                                                      [tookPiece / delivered]│
    └─平均每 90 秒─▶ returningToNest ─▶ inNest(10~35s) ─▶ wandering            食物還在(85%) ─▶ foraging ◀──────┘
                                                                                食物沒了 ─▶ inNest(10~30s) ─▶ wandering
```
- **只有碰到才發現**：`wandering` 每幀檢查與食物的距離（半徑 + 9px），不會朝食物走。
- **招募**（`Colony.recruit`）：通知抵達巢時，招募 4 + 剩餘份數/6 隻；優先叫「在巢裡休息」的（交錯 0.7 秒一隻從洞口出來），不夠再叫離巢 110px 內正在漫步的（直接轉向食物）。每次成功搬回有 50% 機率再多招 1～2 隻。同時處理同一份食物的螞蟻上限約 10（`Colony.maxForagers`；路過自己撞到食物的會直接加入，所以偶爾略多）。
- **圍住**：每隻走到食物邊緣的一個隨機角度（`slot`），停 2～4 秒面向食物，再叼一份走。食物縮小時螞蟻會貼著邊緣移動。
- **走路**：往目標走時加一點左右擺動（`sin`），一條線上的螞蟻看起來像蟻徑；快到目標時（< 30px）不擺動。搬運時速度 ×0.7，通報時 ×1.4，前往食物 ×1.15。
- **回巢休息**：躲在洞裡的螞蟻不繪製（`AntView` 略過 `isHidden`）；穩態下約 15～20% 在巢裡。
- **食物不存檔**（只存蟻窩位置與螞蟻數量）。重新選蟻窩（真的點下新位置）時，食物保留但「已通報」狀態重置。

**驗證**：
- 純邏輯模擬（`Colony` 等單獨編譯，60 隻、30 fps）：蜂蜜放在 250px 外，第 14 秒被發現、35 秒第一次搬回、92 秒搬完（24/24 份送回）；水滴放在 500px 外，58 秒才被發現、126 秒搬完（16/16）。最多約 11 隻同時處理一份食物，任何時候最多 21/60 在巢裡，沒有 NaN。
- App 內用合成事件（`CAMP_TEST_FOOD`）走完整流程：選單放食物 → 點擊放下 → 發現 → 招募 → 搬完；Esc 取消放置也通過。截圖確認蜂蜜與水滴的樣子、巢與食物之間的螞蟻線、叼著金黃色小塊往巢走的螞蟻、放置提示畫面。
- 效能：500 隻 + 一份食物約 12～16% CPU，與之前相當。
- 尚未用真實滑鼠點選單放食物、多螢幕、長時間掛機確認。

**順便修掉的 bug**：命令列傳入的數字設定（`-maxAnts 120`、`-antScale 1.5`）是字串，`Settings` 用 `as? Int / Double` 轉型失敗而退回預設值，所以這類參數之前都沒有生效（`-colorTheme` 是字串所以有效）。改用 `defaults.double(forKey:)`（`Settings.number`）後生效。影響：先前用 `-antScale` 做的螞蟻大小截圖，實際上都是 1.0 倍；效能量測的 `-maxAnts 500` 剛好等於預設值，數字不受影響。

## 角色系統與哥布林（規劃中）

**動機**：使用者不喜歡蟲，想把角色改成哥布林、做成點陣風，之後能自己畫新角色（編輯器）。

**已決定**（2026-09 與使用者確認；首領角色其後改成人類少女，見下）：
- 視角：**四方向**（下、上、右；左 = 右的水平鏡像）。
- 大小：**16×16 像素**一格，顯示約 16～24 點（1 個像素點 = 1.5pt，再乘「大小」設定並吸附到 0.5pt，Retina 上才不會糊）；蟻后／首領用 2.0pt（約 32pt）。
- 螞蟻**留成可選角色**（沿用現有向量畫法），**預設哥布林**；選單可切換。
- 預設角色由 Claude 畫。編輯器只需要畫**正面**，其他方向由編輯器產生（見下方待決定）。
- 哥布林比螞蟻大很多，預設數量上限改按角色決定（哥布林約 150，螞蟻維持 500）。

**角色檔案格式**（內建角色與使用者自己畫的角色相同，才能讓外部點陣編輯器與日後的內建編輯器共用）：

```
Characters/<id>/
├── manifest.json     名稱、影格大小、每個角色（worker / queen）的精靈圖、像素縮放、動畫影格表
├── worker.png        精靈圖：每格 16×16，依序排列
└── queen.png
```
內建角色放在 `Resources/Characters/`（build.sh 複製進 .app）；使用者角色放 `~/Library/Application Support/GoblinCamp/Characters/`。內建精靈圖由 `tools/make_goblin.py` 產生（用程式畫，不依賴外部素材與套件）。

**分步**：
1. 點陣角色引擎：載入精靈圖、依行走方向選圖、走路動畫（依走過距離換影格）、內建哥布林（工人＋首領）、螞蟻變成可選角色、選單「角色」。行為與現有完全相同。
2. 哥布林版行為與用詞：蟻窩改洞穴、「梳理觸角」改撓頭、「產卵」改放蘑菇、睡覺／跳舞／打哈欠專用影格、選單與提示文字用「哥布林」。
3. 角色編輯器：App 內視窗，畫正面（16×16 格、調色盤、走路影格、預覽），存成上述格式。

**待決定（第 3 步前要問使用者）**：「編輯器畫正面，AI 幫我產生其他面」——「AI」指什麼？(a) App 內建的規則式轉換（例：由正面推出背面＝去掉臉部細節，側面＝壓窄並加鼻子），不需網路，品質有限；(b) 呼叫圖像／語言模型 API，效果可能較好，但需要 API 金鑰、網路與費用，且要處理隱私（圖片送出去）；(c) 先只做 (a)，留擴充點給 (b)。

### 第 1 步完成紀錄（點陣角色引擎）

- **角色**：`Character.swift` 載入資料夾（`manifest.json` + PNG 精靈圖）；內建 `Resources/Characters/goblin/`（由 `tools/make_goblin.py` 畫：哥布林 worker、人類少女 queen、頭像 icon）。使用者角色放 `~/Library/Application Support/GoblinCamp/Characters/`。螞蟻是內建的「向量角色」（`Characters.ants`）。選單「角色」切換，設定存在 `character`。
- **首領改成人類少女**（使用者要求；「哥布林抓走人類美少女很正常」）：16×16 Q 版，金髮、小皇冠、藍洋裝，全年齡向。角色檔案裡仍叫 `queen` role，程式內部名稱不變。因為人類少女不適合「產卵」，該動作在像素角色上改成**在腳邊放一朵小花**（`Colony` 收到 `layEgg` 事件時改放花、`AntView.drawFlower`；螞蟻角色仍是蛋）。
- **繪製**：`AntView.drawSpriteWorkers/drawSpriteQueen`。方向由 heading 決定（上下優先於左右要多 15%，避免斜走時來回閃）；走路影格依走過距離換（`legPhase / 4`），停下時顯示第 0 格；用 `interpolationQuality = .none` 保持像素銳利；像素大小吸附到 0.5pt。左向由載入時鏡像產生。探頭／爬出洞口用裁切：探頭是精靈從洞口下方升起，爬出洞口用沿洞口的半平面裁切。搬運的食物顯示在頭頂。
- **數量上限依角色分開記**（`maxAnts.<角色 id>`；螞蟻沿用舊的 `maxAnts` 鍵），預設哥布林 150、螞蟻 500——避免哥布林繼承使用者為螞蟻設的 1000。
- **效能**（Retina，量測有 ±3% 雜訊）：哥布林 50 隻約 7%、150 隻約 5%、500 隻約 7%（貼圖繪製加上約 18% 躲在巢裡不畫，反而比向量螞蟻省）。
- **尚未做**（第 2 步）：專用影格（撓頭、睡覺、打哈欠、跳舞、迎接）；營地改洞穴；蟻后動作裡的觸角、頭變大等只對螞蟻有效，像素角色上暫時沒有對應動畫。

### 改名、圖示與搬家

- 名稱：**哥布林營地（Goblin Camp）**——顯示名稱、`GoblinCamp.app`、套件與執行檔 `GoblinCamp`、原始碼資料夾 `Sources/GoblinCamp`、識別碼 `dev.goblincamp.game`、資料夾 `Application Support/GoblinCamp`、測試環境變數前綴 `CAMP_`（原本 `ANT_`）。程式內部的 `Ant`、`AntView` 等型別名稱**沒有改**：螞蟻仍是可選角色，`Ant` 也可當「居民」通稱。GitHub repo 名稱（`mac-ant-game`）與本機資料夾由使用者自己決定要不要改。
- **舊資料搬家**（`Migration.swift`，`main.swift` 第一行執行）：第一次啟動時，把 `Application Support/AntFarm/` 中新資料夾缺少的檔案複製過去，並把選單設定（生成速度、上限、大小、速度、顏色、存檔開關、營地圖片大小）從舊網域 `dev.goblincamp.antfarm` 複製到新網域。只複製、不刪除；用 `migratedFromAntFarm` 旗標避免重複。已用真實資料測過（存檔與設定都搬成功）。
- **選單列圖示**：原本用 👺，那在 Apple 的字型上是紅色天狗面具，不是綠色哥布林。改成角色自己的頭像（`manifest.json` 的 `icon`，16×16 像素圖，以 16pt 顯示對應 32 個裝置像素，保持銳利）；螞蟻角色沒有圖示，仍用 🐜。（選單列圖示的視窗在這個環境抓不到截圖，只確認了載入成功。）
- **App 圖示**：`tools/make_icons.py` 用哥布林頭像畫 64×64 像素圖（圓角底板、淡淡的光暈、頭像 2 倍），最近鄰放大到 1024，其他尺寸用方塊平均縮小，再用 `iconutil` 包成 `Resources/AppIcon.icns`；`build.sh` 複製進 `.app`，`Info.plist` 設 `CFBundleIconFile`。
- **選單用詞隨角色變**：`Character.noun`（哥布林／螞蟻）、`nestName`（營地／蟻窩）；「顏色」選項只有螞蟻角色顯示。

## 哥布林品種與職業（討論中，尚未實作）

**使用者想法**：原本的哥布林是「平民哥布林」；之後想有不同品種，最終有職業（士兵、戰士、魔法師、弓箭手…）。職業放後面，**先做「有一定機率生出不同身體數值的哥布林」**，之後才可能進化或訓練成不同職業。

**提案**（Claude，2026-09）：

1. **每隻都有自己的特質**：出生時抽一組 `traits`（速度倍率、體型、感知半徑、搬運量、體力／休息傾向、招募能力…）。目前每隻只有隨機的速度（15–40），全部行為相同；先把「個體」資料立起來，之後品種、成長、職業都掛在同一份資料上。
2. **品種 = 一張機率表 + 一組數值範圍 + 一套外觀**。例：平民（多數）、瘦小敏捷（快、感知半徑大、一次只搬 1 份）、壯碩（慢、一次搬 2 份、體型大）、聰明（易發現食物、招募多）、稀有金皮（很少見，全面小加成）。數值影響要「看得出來但不誇張」，並讓存檔記住每一隻。
3. **外觀差異用程式生成**：`tools/make_goblin.py` 已是程式畫的，把身體寬高、耳朵大小、膚色、衣服色、配件變成參數，就能產出多個品種的精靈圖（同一格式，manifest 裡列出多個 worker 變體與權重）。個體的小差異用載入時預先做好的幾種膚色色調。體型用不同格數的精靈（例如 14／16／20 格）而不是縮放，像素才不會糊。
4. **職業 = 裝備疊圖 + 行為模組 + 數值修正**。職業用疊在身體上的小圖（帽、杖、弓、盾）組合，不必每個職業重畫全身；行為則掛在 `Ant.Mode` 上（例如守衛站在洞口附近、弓箭手練習射標靶）。
5. **存檔**：`state.json` 目前只存數量；改成存每隻的 `{品種, 數值, 經驗, 職業}`（位置不用存）。舊存檔沒有這欄位時，全部視為平民。
6. **分期**：A 個體特質與品種（機率、數值、外觀、存檔、選單顯示各品種數量）→ B 成長與經驗（幼年→成年、搬運累積經驗）→ C 職業與新玩法（需要「敵人／事件」等能讓職業有用武之地的東西）。

**要使用者決定的**：
- 職業要做什麼？目前遊戲裡沒有敵人，戰士／弓箭手沒有對象。要不要加入敵人或事件（例如史萊姆、老鼠從螢幕邊緣闖進來、寶箱、任務），還是職業只做成「不同的日常行為」（守衛、練功、施法特效）？
- 稀有品種怎麼來：純隨機，還是受影響（食物搬得多、某種食物、少女的祝福、放置的時間）？
- 是否要能看到每隻的數值？（例：選單的品種統計、「檢視模式」點一隻顯示資訊卡。）
- 數值影響的強度：偏裝飾（微調）還是明顯改變覓食效率？
- 品種與職業的資料格式是否也開放給使用者自訂（manifest 列出品種、權重、數值）？這會和角色編輯器一起設計。

## 使用者的決定（2026-09，品種與職業討論的後續）

1. 未來會偶爾在營地旁出現**敵人、怪物、寶箱**，也可能有想殺死或拯救少女的**人類**；哥布林也可能被殺死。
2. 稀有品種受**累積食物**與**偶發隨機**影響。
3. 要看得到數值：選單開啟後，**畫面右側出現直條視窗**，可瀏覽目前所有哥布林。
4. 數值要**真的影響動作**。
5. 哥布林會**老死**：壽命依品種不同，平民基準 **1 天**（之後升級可延長）；先玩看看。
6. 生成速度選項：10 秒、30 秒、1 分鐘、3 分鐘、5 分鐘，**可自訂**。營地圖要**重畫得好看**，並提供**幾種預設營地**。

**實作順序**：① 生成速度選單（含自訂）② 個體特質、品種、數值影響、老死、存檔 ③ 品種外觀 ④ 右側名冊視窗（點一隻在畫面上圈出來）⑤ 營地重畫與預設營地（隨數量成長）。敵人／怪物／人類／寶箱／職業留到之後。

## 平常哥布林在螢幕上做什麼（討論中）

**問題**：現在除了使用者放食物，哥布林平常只是漫步與回巢休息，畫面沒有「自己發生的事」。

**Claude 的建議**：
- 原則：(a) 事情**自己**偶爾發生，不必使用者一直放東西；(b) 盡量重用現有的「碰到才發現 → 通知 → 圍住 → 搬回」流程；(c) 很安靜，頻率可調（少／普通／多／關閉），不要干擾工作；(d) 保持可愛，不血腥。
- 先做「**可再生的自然資源**」：隨機長出果樹／灌木叢（果實吃完會慢慢長回來）、蘑菇（冒出來、久了消失）。只需要一個「世界事件產生器」加上現有的食物系統。累積搬回的食物量同時驅動稀有品種機率。
- 再做「**小動物**」（羊、豬、雞）：引入通用的 `Creature`（生命值、漫步、逃跑）。哥布林**只有碰到才發現**，發現後通知同伴、一起圍捕，打倒後掉大塊「肉」（份量大、需要多次搬運）。這是戰鬥的雛形——速度決定追不追得到、力量決定傷害，品種數值第一次真正派上用場。
- 之後**敵人、怪物、人類**沿用同一套 `Creature`，只是多了敵意與攻擊哥布林（會死人）；弓箭手／戰士／魔法師才有用武之地。寶箱是最簡單的一種（開了給獎勵或提高稀有機率）。
- 動物與敵人建議從螢幕邊緣走進來、閒晃一陣子、沒人理就走出去；同時在螢幕上的數量有上限（1～3 個）。

## 本輪完成與方向調整（2026-09）

**完成**：
- 生成速度選單（10 秒／30 秒／1 分鐘／3 分鐘／5 分鐘／自訂，可輸入 `45`、`30s`、`5m`、`1.5h`、`90秒`；1 秒到 24 小時）。
- **個體與品種**（`Breed.swift`）：每隻有編號、品種、年齡、種子（決定小差異）與特質；出生時抽品種（`Breeding.roll`：平民多數，稀有品種的機率隨累積搬回的食物最多提高到約 3 倍，另有 2% 隨機突變）；五個品種——平民、敏捷、壯碩、聰明、金皮（清單、比例、數值、說明寫在 `Resources/Characters/goblin/manifest.json`，由 `tools/make_goblin.py` 產生）。
- **數值影響動作**：速度、感知半徑（發現食物的距離）、休息頻率、一次搬幾份（`Ant.Mode.hauling(pieces:)`，`Colony` 取「搬運量」與剩餘量的較小者）、通知時多叫幾隻、壽命。
- **老死**：壽命 = 基準（1 天，`CAMP_LIFESPAN` 可覆蓋）× 品種 × 個人差異；年齡按真實時間累積（不受移動速度影響，`CAMP_TIME_SCALE` 可加速測試）；最後 10% 走得慢；到期先淡出（2.5 秒，`Ant.Mode.dying`）再離開，躲在營地裡的直接悄悄離開；死亡後空位讓出生繼續。
- **存檔**：`state.json` 多存每一隻的 `{id, breed, age, seed}`、累積搬回的食物量、下一個編號；舊存檔（只有數量）讀成「全是平民、年紀輕」；每 60 秒與有變化時存檔。
- **名冊**（`RosterPanel.swift`）：選單「名冊…」在主螢幕右側開直條視窗（浮在透明覆蓋層之上）：總覽、清單（編號／品種／剩餘壽命條／數值）、選中那隻的詳細資料；點一隻，畫面上出現旋轉的虛線圈（`Colony.selectedAntID`）。
- **營地**（`Camp.swift`、`tools/make_camps.py`、`Resources/Camps/`）：四種像素營地（土堆洞穴、岩洞、樹洞、營帳），各三個成長階段（0／30／90 隻，圖與入口座標寫在各自的 `manifest.json`）；選單「營地外觀」切換，可用自己的圖片；設定鍵 `camp`（舊使用者若已有自己的圖片，預設就是「自己的圖片」）。舊的向量土堆繪製已移除。公主站的位置改到營地旁（入口右方 30px），編輯模式的抓取範圍依營地大小。
- **移除螞蟻**（使用者決定「先專心做哥布林」）：拿掉螞蟻角色、向量畫法（`addAnt`、`paint`…）、顏色設定、大小與移動速度選單（一律用預設值，只保留命令列覆蓋）、公主的觸角與打哈欠變大。
- 公主的螞蟻式動作（繞洞口壓土、挖土、搬小石頭）已移除，見 QUEEN_BEHAVIORS.md。

**效能**（Retina）：哥布林 50 隻約 7%、150 隻約 5%、500 隻約 7%。

**內容方向與界線**（使用者提出過的想法與 Claude 的回應）：
- 公主的動作改成人類公主的日常（喝茶、運動、看書、澆花、梳頭髮、唱歌、跳舞、睡覺…），見 QUEEN_BEHAVIORS.md「接下來」。
- **世界觀**（使用者 2026-09）：哥布林是邪惡的一方，抓走公主；之後會有人類冒險者來救她，哥布林也會被殺。這條線保留，是之後「敵人／冒險者／職業」的基礎。
- **哥布林的來源待決定**：可以是營地自己冒出來的（例如冒泡的魔法鍋、蘑菇圈、營火裡、洞穴深處）。
- **開場（已做）**：放好營地後，兩隻哥布林從最近的螢幕邊把公主扛進營地（`Colony.beginCarrying`、`Queen.carried`；公主橫躺在兩隻頭上，`AntView.drawCarriedPrincess`，速度 50 pt/s，一前一後相距 18pt），放下後這兩隻成為最初的居民，才開始計時出生。模擬驗證：四種營地位置（中央、近左、近上、近右）起點都在最近螢幕邊外 30pt，放下時距離公主的家 80pt 內，兩隻間距固定，之後正常出生；還原存檔時沒有開場。

## 公主換裝（2026-09）

- 使用者希望公主有不同款式的衣服（無袖、短褲、短裙、長裙、帽子），不只是顏色。做法：`tools/make_goblin.py` 的公主段改成「同一張臉與頭髮 + `OUTFITS` 服裝設定」（上身袖型、下身種類、帽子、顏色），輸出 7 個精靈圖；`Character.outfits` 載入，`Colony.outfitIndex` 記目前穿哪套，`AntView` 依它選圖。
- 換裝動作：`Queen.changingOutfit`（1.8 秒，轉兩圈、`Decoration.sparkles` 星星），轉到一半（進度 50%）發出 `outfitChange` 事件，`Colony` 換成另一套（一定不同）；機率 3%，約每小時 18 次。測試用 `CAMP_OUTFIT=編號`（起始服裝）與 `CAMP_QUEEN_FORCE=outfit`。
- 驗證：邏輯模擬（每次換裝恰好發出一次事件、發生在轉圈中段、之後回到休息）；實機截圖確認運動裝與冬季外套。未逐一實機檢查全部 7 套（預覽圖確認過）。
- 內容界線同前：日常衣著，不做泳裝、內衣、更衣過程或裸露。
- 注意：使用者另請一個 agent（Opus）重畫角色，`tools/make_goblin.py` 的哥布林段（`Build`、`Look`、各品種體型）是那邊改的，這次沒有動；公主段是這邊改的，兩邊若同時改同一段會互相覆蓋。

## 公主的日常、暫時隱藏（2026-09）

**已做**：
- **暫時隱藏**（使用者：開會、專注工作時要能藏起來）：選單「暫時隱藏 ▸ 30 分鐘／1 小時／2 小時／直到我取消」；隱藏時 `AppDelegate.hide(for:)` 把覆蓋視窗 `orderOut`、名冊關閉、不再 `tick`（營地凍結、年齡不算）、選單列圖示變暗；`unhide()` 恢復（並重設計時避免一次補算）。`CAMP_TEST_HIDE` 可測。
- **公主的日常活動**：喝茶、運動、看書、澆花、梳頭髮、唱歌、跳舞、揮手、打哈欠、想事情，加上躺著睡覺。姿勢圖由 `tools/make_goblin.py` 的 `POSES` 產生（每套服裝都有，接在走路圖下方，manifest `queen.poses` 標出格號）；`SpriteRole.poseImage` 載入；`Queen.Activity`（時長、影格速度、合適服裝、道具）、`Queen.pose`、`Queen.prop`；`AntView.drawProp` 畫茶几、瑜珈墊、花盆、床，`drawDecoration` 畫蒸氣與音符。
- **依活動換衣**：`Queen.startOrChange`——開始活動前若不是合適的服裝，30% 機率先轉圈換上；`Surroundings.outfit` 告訴她現在穿什麼；`Event.outfitChange(to:)` 可指定要換成哪一套。
- 驗證：三小時邏輯模擬、各活動實機截圖、隱藏功能的合成測試；Colony 相關 16 項與開場 21 項測試通過（舊測試「12 秒內出生」的假設因開場需要約 10 秒而改成 26 秒）。

**使用者接著要做的三件事（2026-09）**：
1. 公主的日常（已做）。
2. **自動出現的動物與果樹**（全部自動，不用使用者放）：螢幕上偶爾出現小動物；營地附近會長出果樹。設計見前面「平常哥布林在螢幕上做什麼」——動物用通用 `Creature`（生命值、漫步、逃跑、掉肉），打獵有風險（使用者同意）；果樹是可再生的食物來源，重用現有的食物流程；頻率可調（少／普通／多／關閉）、數量有上限。**已實作，見下方「自然事件」**。
3. **Claude 通知**（使用者：「這樣才有結合工作性」）：電腦上任何 Claude 需要通知時（例如等待許可、做完了），跳出哥布林並用哥布林的聲音告知。設計草案：
   - 觸發：Claude Code 的 hook（`Notification`、`Stop` 等）呼叫一個小腳本，透過自訂網址 `goblincamp://notify?...`（`open -g`）把事件交給 App；App 需登錄 URL scheme 並處理開啟網址。
   - 呈現：一隻哥布林從營地跑到畫面前面，頭上冒出對話框（顯示文字），並用哥布林風格的聲音念出來（`AVSpeechSynthesizer` 調高音調與語速，加上程式合成的哥布林嘻笑音效，不需外部音檔）。
   - 與「暫時隱藏」結合：隱藏或會議中不彈出、不出聲，只讓選單列圖示亮一個提示點；取消隱藏後可看最近幾則。
   - 需要使用者決定：要通知哪些事件（等許可／完成／兩者）、是否同意修改 `~/.claude/settings.json` 加 hook（全域設定，未經同意不動）、聲音要不要可調（音量、關閉語音只留音效）。**尚未實作**。


## 自然事件：動物、果樹、打獵（2026-09 完成）

全部自動發生，選單「自然事件（動物、果樹）」可選 關閉／少／普通／多（預設普通，`Settings.wildlife`）。

**果樹**（`Colony.spawnTree`）
- 營地開始後不久，在營地周圍 90～260px 長出果樹（最多 2／3／4 棵，間隔約 10／5／2.5 分鐘），至少離其他東西 60px。
- 樹是一種 `FoodSource`（`origin == .tree`，最多 4～6 顆果實）：哥布林**碰到才發現**，回巢報信、招募、圍著搬，流程與水、蜂蜜相同。
- 果實被摘光後樹不會消失，每約 70 秒長回一顆（`Colony.treeRegrowTime`），長出來後又要重新被發現。
- 「清除所有食物」不會清掉樹。

**動物**（`Animal.swift`、`Colony.spawnAnimal`）
- 雞、羊、豬（`Resources/Animals/`，`tools/make_animals.py` 產生，資料在各自的 manifest：生命、速度、肉量、是否兇、出現權重）。
- 從螢幕隨機一邊走進來，閒晃 2～4 分鐘，沒人理就走出螢幕；同時最多 1／2／3 隻，間隔約 8／4／2 分鐘。
- 哥布林**走近才發現**（`.foundCreature`）→ 跑回巢報信（`huntNews`）→ 巢裡休息的哥布林與附近閒逛的去圍捕，最多 8 隻；追到就攻擊（傷害＝力量 `might`）。
- **風險**：雞、羊被打會逃跑 2.5 秒；豬會反擊（每次被打 40% 機率咬中 1 點）。哥布林生命 `health`，≤1 會受傷、頭上出現紅十字、退回巢裡休養；≤0 就死掉（名冊統計「打獵犧牲」）。
- 打倒後留下一堆肉（雞 5、羊 12、豬 16 份），參與圍捕的直接轉去搬肉，其他人走近才會發現，但已知道位置的不用再報信。
- 動物與樹的狀態不存檔（重開就重新出現）。

**測試**：`CAMP_WILD_SCALE=倍數` 讓自然事件快轉；`CAMP_TEST_WILD=種類,dx,dy` 在營地旁放一隻動物與一棵樹並每 3 秒印狀態。已確認：樹被發現、搬完、長回果實；羊、豬被圍捕、留下肉並被搬完；自動生成動物與樹的 log。

## Claude 通知（2026-09 完成）

使用方式見 README「Claude 通知」。設計重點：
- 走 URL scheme（`goblincamp://notify`），不需要任何權限；Info.plist 登記，`build.sh` 用 lsregister 註冊。任何程式都能開這個連結，所以只接受短的專案名和格式受限的 app id，並有 6 秒節流、最多排 3 個。
- 彈出者：`MessageStage`（佇列）＋ `Message`，畫在 `AntView.drawMessage`，不屬於模擬，暫停時也會出現；位置是游標所在螢幕的右上角（選單列下方）。
- 說話者：25% 公主，其餘依營地裡真實存在的哥布林品種挑；每個 `Speaker` 有自己的音高、語速、前置音效與台詞，氣泡標題用 `Speaker.title`（之後可換成哥布林自己的名字）。
- 聲音：程式合成的短音效（`Synth`：嘻笑、尖叫、悶哼、嗯哼、鈴聲、閃亮）加上 macOS 的 zh-TW 語音（Meijia、Eddy、Flo、Grandpa、Grandma、Shelley…）。**只驗證過音效檔正確、語音存在，沒有實際聽過，音高與語氣可能需要聽了再調。**
- 點泡泡：另開一個很小的接收點擊視窗（總覽視窗全部穿透），點了就啟動 hook 傳來的 app（依 `TERM_PROGRAM` 對應 Terminal／iTerm／VS Code／Warp／Ghostty／WezTerm）。
- 暫時隱藏時：不跳出、不出聲，選單列圖示旁顯示「● 數量」。
- 待決定：**直接在泡泡裡回覆 Claude**。做法有兩種：（1）PermissionRequest hook，泡泡帶「允許／拒絕」按鈕；（2）Stop hook，泡泡帶輸入框，輸入的話當成 Claude 的下一個指示。兩者都要 hook 等你回答（會擋住 Claude 直到你按），需要你同意。
- **Hook 設定還沒裝**：寫入 `~/.claude/settings.json` 被權限擋下，等使用者自己加（片段在 README）。

## 番茄鐘（2026-09 完成）

見 README「番茄鐘」。以 `Date` 計時（不受暫停或睡眠影響）；哥布林的品種決定說話的語氣（`Speakers.pomodoroLine`）。已驗證：開始、轉入休息、結束走開三個事件與畫面（專注綠色／最後一分鐘紅色、休息藍色）。還沒做：暫停鍵、連續多輪（長休息）、每日完成統計。


## 專注模式與工作模式（2026-09，取代「暫時隱藏」）

- 使用者的定義：**專注模式**＝完全暫停（原本的「暫時隱藏」）；**工作模式**＝背景繼續運行、只是不出現在畫面上。
- 實作：`AppDelegate.QuietMode { focus, work }`、`hide(_:for:)`；`isHiddenByUser`（視窗藏起來、不跳通知）與 `isFrozen`（只有專注模式，不 `tick`）。兩種模式的選單各有 30 分鐘／1 小時／2 小時／直到我取消。已驗證：專注模式下哥布林數不變，工作模式下背景繼續出生。
- 使用者說明：他打算長時間常駐，平常主要靠番茄鐘與 Claude 通知。所以整理了 [SCENARIOS.md](SCENARIOS.md)，並提出「工作模式下番茄鐘與通知仍要出現」的待決定事項。


## 四種狀態、全螢幕專注、第二螢幕、每日統計、hook 安裝（2026-09）

見 SCENARIOS.md。使用者的四種需求：完全靜音（專注模式）、有番茄鐘和通知但營地暫停（節能模式）、有番茄鐘和通知且營地背景運作（工作模式）、全開。實作：`QuietMode { work, saver, focus }`、`effectiveMode`（全螢幕視窗會強制專注）、`isHiddenByUser`（營地不畫）、`isSilenced`（不跳出、不出聲）、`isFrozen`（不 tick）；營地隱藏時覆蓋視窗仍在，`AntView` 只畫番茄鐘與通知（`colony.campHidden`）。`Stats.swift` 存每日統計（測試時 `CAMP_NO_SAVE` 不寫檔）。`tools/install-hooks.sh` 安裝／移除 Claude Code hook。已驗證：三種模式的視窗、暫停、通知、更新頻率；全螢幕開啟與結束；統計計數。沒驗證：真實第二螢幕、真實全螢幕影片或簡報、開機自動啟動。


## 切換模式、預設工作模式、README 美術（2026-09）

- 模式（`AppDelegate.setMode`）：選單最上面四個一鍵切換的項目（全開、工作、節能、專注）＋「切換模式後持續」＋「啟動時的模式」（預設工作模式；還沒有營地時固定全開，好讓人選位置）；全域快捷鍵 ⌃⌥1～4（`HotKeys.swift`，用系統熱鍵服務，不需要權限，已用真的按鍵驗證）。選單列圖示旁顯示模式小字（工、省、靜）。專注模式下番茄鐘時間到，圖示閃動；回到全開時有靜音的「歡迎回來」小泡泡（營地多了幾隻、錯過幾則通知）。開始番茄鐘時若在全開，自動進入工作模式，結束後回來。
- README 美術：`tools/pixelart.py`、`tools/make_readme_art.py` 從遊戲自己的精靈圖合成 `docs/images/` 八張圖（封面、品種、公主、營地、自然事件、四種狀態、番茄鐘、通知），全是 4 倍放大的點陣，重新產生只要執行 `python3 tools/make_readme_art.py`。


## 泡泡上直接回覆 Claude（2026-09）

- 機制（已對照本機 Claude Code 2.1.278 的程式確認格式）：`PermissionRequest` hook 輸出 `{"hookSpecificOutput":{"hookEventName":"PermissionRequest","decision":{"behavior":"allow"}}}`（拒絕是 `"behavior":"deny"` 加 `message`）；`Stop` hook 輸出 `{"decision":"block","reason":"…"}` 讓 Claude 把 reason 當作下一個指示繼續；什麼都不印就照平常。
- `tools/goblin-ask.sh permission|reply`：開 `goblincamp://ask?…`，等遊戲在 `Application Support/GoblinCamp/replies/` 寫下 `<id>.ack`（4 秒內沒有就放棄，不卡住 Claude Code）與 `<id>.json`（答案），再印出 hook 要的 JSON。id 是一次性的隨機編號，遊戲只接受 `[A-Za-z0-9-]{8,64}`。
- 遊戲端：`AppDelegate.handleAsk` 決定要不要顯示問題（專注模式、關掉回覆、佇列滿了都立刻回「none」），`AskPanel`（真的視窗、不搶焦點的面板）放按鈕與輸入框；按鈕是自己畫的 `PillButton`，因為系統按鈕在深色模式下是白底白字。逾時、關掉或被清除的問題都回「none」。
- `tools/install-hooks.sh` 預設裝 `PermissionRequest`（timeout 70）、`Stop`（timeout 70）、`Notification`（只有 idle_prompt、elicitation_dialog）；`--simple` 只裝一般提醒。
- 已驗證：允許、拒絕、自己去看、輸入回覆、不用了五種情況，腳本輸出的 JSON 正確；泡泡畫面（`CAMP_TEST_ASKSHOT` 畫成 PNG）。沒驗證：真的 Claude Code 觸發 hook 後是否照預期繼續、輸入框在真實使用時取得鍵盤焦點（用真滑鼠點）的行為。


## 選單一鍵連接 Claude Code（2026-09，給同事用）

- 目的：同事不需要開終端機。選單「連接 Claude Code」：說明視窗 → 按「連接」→ `HookInstaller` 寫 `~/.claude/hooks/goblincamp-hook.sh`（記著遊戲目前的位置，找不到就安靜退出）、備份並更新 `settings.json`（只加減自己的 hook，用 `goblincamp-hook.sh`、`goblin-notify.sh`、`goblin-ask.sh` 當記號，舊版寫的也會一起換掉）；也可以只裝一般提醒、或移除。設定檔不是正確 JSON 時直接停下。從「App Translocation」的暫時位置執行時，請使用者先搬到「應用程式」。
- 原本的 Python 腳本改成遊戲執行檔的 `--hook` 模式（`HookCLI.swift`，不依賴 Python），`tools/goblin-*.sh` 只留成轉接腳本，讓舊設定還能用；`tools/install-hooks.sh` 移除。
- 已驗證：對假的 `~/.claude`（`CAMP_CLAUDE_DIR`）安裝、重複安裝、只裝提醒、移除，其他設定與別人的 hook 都保留；`--hook` 五種情況的輸出（允許、拒絕、自己去看、回覆、遊戲沒開時立刻結束）。沒驗證：用真滑鼠按選單的實際視窗，以及真的 Claude Code 載入這些 hook。


## 版本、說明手冊、名字（2026-09，v0.2.0）

- **版本**：`Info.plist` 升到 0.2.0（build 2）；選單「關於哥布林營地（v0.2.0）」用系統的關於視窗；說明手冊標題也顯示版本。之後每次發新版要改 `CFBundleShortVersionString`，`./build.sh --release` 的 zip 檔名會跟著變。
- **說明手冊**：選單「說明手冊…」，`ManualWindow.swift`，內容寫在 `sections`（開始玩、四種狀態、番茄鐘、連接 Claude Code、哥布林與公主、營地與自然事件、資料更新移除、常見問題），改功能時要記得同步改這裡。已畫成 PNG 確認排版。
- **名字**：`Names.swift`。哥布林的名字由自己的 seed 產生（兩個音節加偶爾一個字尾），所以重開後還是同一個名字；被玩家改過的才存進 `state.json`（`SavedGoblin.name`，選填，舊存檔讀得進）。公主的名字存在 `SavedState.princessName`（選填）。第一次玩（新營地）時，放好營地後遊戲先暫停、問公主的名字（好／換一個／先叫她公主）；舊存檔第一次打開新版時也會問一次；名冊可以連按兩下改哥布林名字；選中的哥布林頭上會顯示名字；通知泡泡標題用「名字・品種」。
- 已驗證：名字存檔與重新載入（用 `CAMP_DATA_DIR` 隔離）、手冊排版。**沒驗證**：實際看到「幫公主取名字」的對話框並打字（測試模式會略過它）、名冊裡連按兩下改名、關於視窗。
- 還沒做（想法）：哥布林名字出現在番茄鐘的哥布林旁、通知台詞偶爾叫名字、幫營地取名、更多名字風格。

- **名字更多元（v0.2.0）**：`Names.goblin(seed:)` 有四種樣式：兩個聲音（可加字尾）、三個聲音、外貌（歪鼻、老禿頭）、泥巴森林系（泥巴、蘑菇頭），共 **34,944 種**組合（`Names.combinationCount`）；用 splitmix64 打散 seed，20 萬個隨機 seed 實測出現 22,200 種不同名字。出生時如果和在世的哥布林重名，會重抽 seed（500 隻實測 0 個重名）。**每隻的名字都存進存檔**（`SavedGoblin.name`），之後就算改了產生方式，舊哥布林也不會被改名；沒有名字的舊存檔才用 seed 產生。


## 名字一百萬種、允許並記住、桌面與全螢幕（2026-09）

- **名字 > 100 萬種**：`Names.combinationCount` = 1,031,156。名字只是名字：**稱號（疾風、勇者這類）不放進名字**，留給以後的稱號系統。樣式：兩個聲音（咕嚕、噗嘟牙）、三個卡通音節（波米露）、兩個音節加可愛字尾（米露醬、波嘟丸）、聲音加音節、小／老／大／阿／嘟＋兩音節、疊字（波波、米米醬）、外貌（老禿頭）、泥巴森林系（阿蘑菇頭）。2,000,000 個隨機 seed 實測 564,281 種不同名字，1000 隻在世的哥布林 0 重名。
- **允許並記住**：`PermissionRequest` 的輸入有 `permission_suggestions`（Claude Code 自己提供的「不要再問」選項，格式已對照本機 2.1.278 的程式確認）；`HookCLI.rememberable` 只保留能用一句話描述的（addRules 的 allow、setMode acceptEdits、addDirectories），並把 `destination` 一律改成 `session`（只有這次對話，不寫進任何設定檔）再放進 `updatedPermissions`。泡泡多一顆「允許並記住」，並顯示「本次對話都允許：Bash(git push:*)」。已用假的 id 離線測過六種答案的輸出（`CAMP_HOOK_TEST_ID`）。
- **桌面**：`Spaces.swift` 用視窗伺服器的唯讀私有函式（`CGSCopyManagedDisplaySpaces`）讀出每個螢幕現在是第幾個桌面、是不是全螢幕 App 自己的桌面；讀不到就當成所有桌面。預設只在桌面 1 畫營地（`Settings.desktops = [1]`），沒勾選的桌面只畫番茄鐘與通知（`AntView.showsCamp`）。桌面切換用 `activeSpaceDidChangeNotification` 立刻反應，外加每 0.5 秒檢查。已用 `CAMP_FAKE_SPACE=1/2/fs` 模擬驗證畫面（桌面 1 有營地，桌面 2 完全空白，全螢幕桌面進入專注模式、視窗全收）。
- **全螢幕一定不出哥布林**：在全螢幕 App 的桌面裡不畫營地（不管有沒有開「全螢幕時自動專注」），開著時視窗也全部收起、通知靜音。實測發現：只拿掉視窗的 `fullScreenAuxiliary` 並不能讓覆蓋視窗從全螢幕桌面消失（這個系統上視窗仍在），所以真正的保證是上面的偵測。**沒驗證**：真實切換桌面時的瞬間（切換動畫中可能有一兩格還看到哥布林）、多螢幕各自不同桌面的情形。

- **螢幕（家）**：`ScreenChoice.swift`＋設定 `screenMode`（nest／all／list）、`screenNames`。預設 `nest`＝哥布林只住在營地所在的螢幕（家會跟著營地拖曳到別的螢幕），其他螢幕不畫營地（`showsCamp`）也不讓哥布林走過去（`colony.walkable` 只含允許的螢幕，所以動物、食物、被扛進來的公主也都在那個螢幕）；單螢幕時不受影響；勾選清單是空的或都拔掉了就退回所有螢幕。切換時 `Colony.updateWalkable` 會把不在允許螢幕上的哥布林、食物、營地搬到最近的允許螢幕邊緣。已用兩個假螢幕測過規則（`CAMP_TEST_SCREENCHOICE`）。**沒驗證**：真的接兩個螢幕時的畫面與被搬走的哥布林的樣子（我這台只有一個螢幕）。
- 使用者提醒：不要自己 commit，只在被要求時才 commit（已記在專案記憶）。這一輪（名字一百萬種、允許並記住、桌面、螢幕）之後的改動**都還沒 commit**。

## 走動範圍與地圖視窗（想法，尚未實作；2026-09-20 使用者提出）

使用者的想法：哥布林不要蓋滿整個螢幕，希望能選走動範圍——全螢幕、限制在底部或右邊，底部那一條可以有森林之類的背景；或是給一個獨立的視窗，全部哥布林住在裡面，可以收起來，還能有地圖背景。

- **A 走動範圍**：全螢幕（現況）／底部一條（貼著 Dock，高度可調，橫向捲軸式的地面，背景可以是森林、草地、洞窟，營地在一端）／側邊一條／自訂方塊（拖出來）／避開目前視窗（只在沒被蓋住的地方走，或站在視窗標題列上走）。實作上就是把 `colony.walkable` 換成那塊矩形（`ScreenChoice` 已經是這條路），加上背景繪製；範圍小也更省 CPU。
- **B 獨立營地視窗**：可拖曳、可調大小的小視窗，哥布林都在裡面，背後是地圖；可以收成螢幕邊緣的小旗、選單列圖示或滑鼠移過去才展開，專注模式時自動收起；可以固定在某個桌面、螢幕；因為不會蓋到別的視窗，可以直接用滑鼠互動（拖曳、餵食、點一下看名字）。地圖主題：森林空地、岩洞、沼澤、雪地、沙漠、夜晚營火。這也是之後冒險者、敵人、寶箱最適合的舞台。
- **C 延伸**：日夜與天氣（跟著電腦時間，夜晚圍營火、下雨躲進營地）、地形互動（池塘釣魚游泳、大樹採果、柵欄旗子，營地長大時周圍跟著多東西）、番茄鐘休息時的營火晚會、動物與敵人沿著地圖的路線進來、季節與節日。
- **建議順序**：第 1 步走動範圍選單＋底部森林背景（改動小）→ 第 2 步獨立營地視窗（最像遊戲）→ 第 3 步日夜、天氣、地形。

## 走動範圍、森林背景、營地視窗、防閃爍（2026-09-20，第 1、2、3 步）

- **走動範圍**：`Settings.rangeMode`（screen／bottom／right／left／window）、`rangeSize`、`scenery`。`ScreenChoice.range` 算出一條的範圍（避開選單列與 Dock，用 `visibleFrame`），`walkBand` 取靠螢幕邊的外側一半當成哥布林真正走的草地（樹在後面）；營地會放在那一條的中央；動物與被扛進來的公主只從那一條的兩端進來（`Colony.entrySides`），順手修掉範圍太窄時 `random(in:)` 會當機的問題。`placeNest` 一開始就把營地放到真正會在的位置，扛人的哥布林才不會走去舊位置卡住。
- **背景**：`tools/make_scenery.py` 畫森林（松樹、圓樹、灌木、蘑菇、草地）與草地兩種橫向可重複的圖，另外旋轉出左右兩邊的版本、以及營地視窗的草地磚；`Scenery.swift` 用整數倍放大貼滿那一條，並依電腦的時間上色（傍晚橘、夜晚藍、清晨粉，只上在有畫的像素）。
- **營地視窗**：`MapWindow.swift`。營地有自己固定原點 (0,0) 的小世界，視窗只是它的視口，所以拖動視窗不會影響任何人；縮放視窗就是改世界大小，`Colony.updateWalkable`／`relocate` 會把大家搬進來。關閉鈕＝收起（`mapCollapsed`），哥布林仍在背景生活；工作／節能／專注模式、沒勾選的桌面、全螢幕桌面時自動收起；選位置、放食物、編輯營地時會自動叫出來。視窗位置與大小會記住。
- **防閃爍**（使用者說滑到其他螢幕時哥布林會閃爍消失）：找不到畫面上的原因（這台只有一個螢幕），所以做了幾個防護：①全螢幕只看「哥布林住的螢幕」，別的螢幕上的全螢幕影片不再讓全部哥布林消失；②輪詢得到的變化要連續兩次（約 1 秒）都一樣才採用，系統主動通知的變化（切桌面、螢幕改變）才立刻套用，避免來回跳；③通知泡泡不再挑到正在放全螢幕影片的螢幕；④選單「複製診斷資訊」會把最近的顯示／隱藏變化、每個螢幕的桌面與全螢幕狀態、設定複製到剪貼簿。**沒驗證**：真的多螢幕上是否已經不閃了，要使用者實際用一次；還會閃的話請用「複製診斷資訊」貼給我。
- **已驗證**：底部一條（白天、夜晚）、右邊一條的畫面；營地視窗的畫面（含傍晚變色、營地在正中央）；選單項目建得起來。**沒驗證**：左邊一條、Dock 在底部以外的位置、營地視窗在多桌面／多螢幕間移動時的行為、真滑鼠拖曳視窗與縮放。
- 尚未做（第 3 步剩下的）：~~天氣、池塘、營火晚會~~（見下面「B → D → E」）。

## 第二輪修正：太大、右邊方向、營地視窗、切桌面不能出現（2026-09-20 晚）

使用者看了上一輪：底部一條「超大，根本沒法工作」、右邊一條的樹橫著長、營地視窗縮小（縮到 Dock）會馬上彈回來、拉大後草地外沒有樹、切到別的桌面時哥布林還會出現大約一秒（附螢幕錄影）。

- **大小**：樹與草地改成一個藝術像素＝一點（跟哥布林一樣大），不再放大三倍。森林有 30／42／54／64 點四種高度（`forest-N.png`），預設 42；草地是一條 24 點的薄薄一層。哥布林只走靠螢幕邊的草地那一段（一條的 70%，至少 30 點）。
- **右邊、左邊**：不再把橫向的圖轉 90 度，改畫專用的直立版本（`forest_side`）：樹一棵一棵直立往上疊，草地是靠螢幕邊的一整條。
- **營地視窗**：縮到 Dock（`windowDidMiniaturize`）算收起，`show()` 看到縮小的視窗就不動它；四周一圈森林（上面一排樹、左右各一列直立的樹、下面是有草地的邊），拉多大都有；哥布林只走圈裡面的空地（`MapWindow.walkArea`）。
- **切桌面完全不出現**：原本是「覆蓋視窗在所有桌面上，偵測到沒勾選再不畫」，偵測一定慢半拍。改成**把視窗本身放到指定的桌面**：營地的覆蓋視窗（與營地視窗）不再是 `canJoinAllSpaces`，而是用視窗伺服器的 `CGSAddWindowsToSpaces`／`CGSRemoveWindowsFromSpaces` 只放在勾選的桌面（每個螢幕各自的桌面清單），其他桌面與全螢幕桌面根本沒有這個視窗。番茄鐘與通知另外用一個「工具覆蓋視窗」（所有桌面，`AntView.toolsOnly`）。選位置時會暫時把視窗也放到現在這個桌面。設定變更、新增桌面時 0.5 秒內重新指定。這是私有函式；如果不能用，就退回舊做法（視窗在所有桌面，靠不畫來藏）並記在診斷資訊裡。先前的防閃爍（全螢幕只看住的螢幕、連續兩次才採用）仍保留當第二道保險。
- 已驗證：底部一條與右邊一條（很薄、樹直立）的畫面；營地視窗（四周森林、放大也有）；縮到 Dock 後維持縮著；營地覆蓋視窗確實被放到桌面 1 的 Space（`placed on desktops [3]`）；番茄鐘在拆開後的工具視窗裡照常畫；另有獨立的小程式驗證過 `CGSAdd/RemoveWindowsFromSpaces` 真的能改一個視窗所在的桌面。**沒驗證**：真的切到桌面 2／全螢幕桌面時完全沒有畫面（我這裡無法在切桌面時錄影，只能靠系統行為與剛剛的 Space 檢查），請使用者再錄一次確認。

## 一直轉動的哥布林（2026-09-20 晚，使用者的第二段螢幕錄影）

- 使用者說在底部一條和營地視窗裡「有些會一直轉動很怪」，希望依範圍調整移動速度或方式。用測試（`CAMP_TEST_SPIN`：每 0.05 秒記錄每隻的朝向，數 20 秒內朝向變了幾次，並列出最嚴重的幾隻與牠們當時在做什麼）量到：原本平均每秒換 0.5～0.6 次朝向，最嚴重的一隻（回營地途中）2.5 次。
- 兩個原因：①朝向直接由行進角度算，走斜線、左右微擺（回營地時有輕微搖擺）、隨機轉彎時，角度在「側面／正面」的分界線附近來回跳，圖案就閃來閃去；②撞到牆時一律原地轉半圈（`heading += π`），在窄的範圍裡會一直被擋住、一直轉。
- 修正：①`SpriteDirection(heading:previous:)` 加上黏性（角度要明顯越過分界才換），`Ant.facing`、`Queen.facing` 每次更新時算；②撞牆改成像球一樣只反射被擋的那個方向並沿著牆滑；③一條很窄很長的範圍（`AntWorld.axis`）裡把方向限制在長邊的 ±0.6 弧度內，所以是沿著那一條來回巡邏；④走動速度依範圍：整個螢幕 1.0、一條 0.75、小範圍（面積小於 70 萬平方點，例如營地視窗）0.85（`Colony.pace`，只影響閒晃，不影響覓食與搬運）。
- 結果：整個螢幕 0.08、底部一條 0.06、營地視窗 0.11 次／秒，最嚴重的也只有 0.4。**沒驗證**：實際看起來的感覺（要使用者再看一次），以及公主自己的走動。

## 第三輪：卡住轉動、側邊、人數、不擋 Mac 的工具（2026-09-20 夜）

- **旋轉／閃爍**：見上一節（黏性朝向、撞牆反射、沿著一條巡邏、範圍不同速度不同）。側邊另外：橫向擺動範圍縮小（窄的一條 ±0.2 弧度）、撞牆時保持走路動畫（原本撞牆那一格會變成站立，閃一下）。
- **側邊樹木像飄著**：因為樹畫在透明的地方，沒有地面。改成整條寬度都是草地，樹種在草地上、腳邊有陰影（`forest_side`），一條至少 42 寬，哥布林可以在整個寬度上走。
- **右邊看不到公主**：她固定站在營地右邊 30 點，右邊一條的營地在螢幕邊，她就站到螢幕外面了。`Queen.homeSpot(for:walkable:)` 改成依序試右、左、下、上，選第一個在走動範圍裡的位置；用 `CAMP_TEST_SPIN` 的報告確認公主在螢幕內。
- **人數**：`Colony.visibleCap`（約每 2200 平方點一隻，至少 12：整個螢幕 600 以上、營地視窗約 150、一條約 20、側邊約 12）；超過的哥布林在營地裡休息（新出生的直接在營地裡待 25～60 秒），過擠時（`AntWorld.crowd`）回營地休息的機率提高、待久一點，有空位才出來。實測底部 20／20、側邊 12／12，總數 90～130 隻。
- **不跟 Mac 自己的工具打架**：所有我們的視窗改放在 Dock（層級 20）下面一層（`Levels.overlay` = 18、`Levels.panel` = 19）：Dock、選單列、Spotlight、Mission Control、通知橫幅、控制中心一律在上面；對話框、泡泡、名冊、營地視窗（永遠在最上面時）用 19。原本是選單列層級（25），會蓋在 Dock 上。
- **沒驗證**：公主在側邊時各種活動的道具（茶几、瑜珈墊）會不會伸出螢幕；真的看到 Dock、Spotlight、通知橫幅時的層疊（沒辦法在這裡叫出來）；使用者的螢幕是直式時的整體樣子。

## v0.3.0（2026-09-20）

- 版本 0.2.0 → **0.3.0**（build 3）。這一版新增：一百萬種以上的卡通名字、「允許並記住」、一鍵連接 Claude Code 的選單、走動範圍（整個螢幕／底部／右邊／左邊一條／獨立的營地視窗）與森林背景、依電腦時間變色、只放在勾選的桌面（視窗直接放到指定桌面）、哥布林住在哪個螢幕、防旋轉閃爍、範圍小時減少同時走動的人數、不蓋住 Mac 自己的工具（視窗放在 Dock 下面一層）、複製診斷資訊、說明手冊與關於。
- 營地視窗改大小時，同時出來走動的數量會跟著調整（測過：縮小後 24 秒內從 60 降到 17，放大後 29 秒內從 30 升到 103；`CAMP_TEST_MAPRESIZE`）。
- 給同事更新：`./build.sh --release` 會產生 `dist/GoblinCamp-0.3.0.zip`；紀錄、設定、Claude Code 的連接都不會消失（識別碼與資料夾沒變）。
- 已知還沒驗證：真的多螢幕、真的切桌面（一格都不出現）、Dock／Spotlight／通知橫幅的層疊、直式螢幕、側邊一條的朝向變化數字偏高（約 1 次／秒，可能只是出入營地）。


## B → D → E：番茄鐘進階、側邊朝向、天氣與地形（2026-09-20）

使用者指定順序「B -> D -> E」。

- **B 番茄鐘進階**：`Pomodoro.swift` 加入輪數、長休息、暫停、跳過；時鐘字樣 `專注2/3`／`休息`／`長休息`／`暫停`；選單「經典 4 輪」與自訂對話框。休息時營火晚會（`Colony.fire`、`AntView.drawFire`）：營地藏起來時會自動顯示、休息結束回原模式；哥布林被吸引到火堆周圍 30～75 點的環上。測試：`CAMP_TEST_POMODORO=f,r,輪數,長休息`、`CAMP_TEST_PAUSE`。
- **D 側邊一條的朝向**：直立的一條裡哥布林沿著長軸巡邏，朝向用遲滯（`SpriteDirection(heading:previous:)`），碰牆時反射並保持走路動畫，不再閃爍或原地打轉。
- **E 天氣與地形**：`Colony.weather`（前 20～60 分鐘晴天，之後隨機下雨），雨只畫在營地視窗與一條範圍內；下雨時 `visibleCap × 0.35`，哥布林休息機率變高（躲進營地）。池塘：`Pond.swift` 以種子產生**不規則的輪廓**（幾個不同頻率的波再加一個灣／岬，平滑後只留最大的一片水），依離岸距離分四層深淺，岸邊沙土、石頭（單顆或一小堆，帶陰影）、蘆葦、睡蓮，約一半會在最深處挖出一座長著灌木的小島；預先畫成一張點陣圖，`AntView.drawPond` 只再疊上會動的漣漪與閃光。哥布林避開靠 `Pond.blocks`（依真實形狀，不是方框）。形狀每次啟動隨機（`CAMP_POND_SEED` 可固定），視窗縮放時同一個形狀伸縮，不會重新生成。
- 使用者回饋：第一版池塘是正圓形「太醜」，要求不規則、每次不同、可有石頭與小島 → 已改成上面的做法；已畫出多個種子檢查有變化。
- **沒驗證**：真實使用時長時間的下雨／營火／池塘行為；池塘在很窄的視窗（避開營地時）是否偶爾不出現屬預期（視窗寬 <380 或高 <280 不生成）。

## v0.4.0（2026-09-20）

- 版本 0.3.0 → **0.4.0**（build 4）。這一版新增：番茄鐘連續多輪／長休息／暫停／跳過、休息時的營火晚會、偶爾下雨（哥布林躲進營地）、營地視窗裡形狀每次不同的池塘（有石頭、蘆葦，有時有小島）、側邊一條的朝向修正。
- 給同事更新：`./build.sh --release` 會產生 `dist/GoblinCamp-0.4.0.zip`；紀錄與設定都不會消失。

## 魔獸系統第一版（2026-09-20，尚未 commit）

使用者要先做敵人：初期低等魔獸、之後一路規劃幾十種；魔獸掉落素材（**至少三種、不一定掉、各有機率**）、之後做成武器與裝備。使用者決定：哥布林會受傷、回營地休息、公主能治療；公主要被保護；先做框架加史萊姆與巨鼠。之後補充：魔獸除了走路還要有不同的**攻擊動作**、不同魔獸要有不同的血量、被打時要停下來**對打**而不是一直走。

- 完整設計與規劃在 [MONSTERS.md](MONSTERS.md)。做法：魔獸就是「動物」資料夾多了幾個欄位（`hostile`、`hp`、`attackStyle`、`walkFrames`、`drops`…），美術與資料由 `tools/make_monsters.py` 產生。
- 已驗證（測試環境）：兩種魔獸走進來、被圍打（史萊姆分裂）、哥布林受傷回營地、魔獸被打時停下來反擊、公主躲進洞（`inHole`）、掉落搬回營地並存進倉庫、存檔重開後素材還在、倉庫文字。一場戰鬥約 20 秒，150 隻哥布林的營地沒有人犧牲。
- 順手修好的一個 bug：開場「兩隻哥布林抬公主」的時候，如果營地視窗被縮放（公主被直接放下），那兩隻會永遠停在原地。現在公主不在被抬狀態時會放它們走（`Colony.moveCarriedPrincess`）。
- **沒驗證**：全螢幕範圍下魔獸的表現（只在營地視窗與小範圍測過）、長時間使用後的稀有掉落頻率是否合適、攻擊動作在實際大小（1.5 倍點陣）下看起來是否清楚（放大檢查過圖，遊戲內截圖被哥布林群擋住）。血量與機率是初值，之後要調。
- 還沒做：工坊與裝備、蝙蝠／蘑菇怪／石甲蟲／沼澤青蛙、稱號、魔獸來襲的通知泡泡。

### 工坊與裝備（同一天接著做，尚未 commit）

使用者說「先做工坊」和「裝備」，接著要求：哥布林也要有攻擊動作；武器、胸甲、帽子、褲子、鞋子、盾牌、法杖、弓箭、長槍、手甲、短劍、長劍、雙刀、雙手劍、皮甲、布甲都要有基本幾種，而且會持續更新；不一定現在都做得出來，未來可以做也沒關係。

- 設計與配方見 [EQUIPMENT.md](EQUIPMENT.md)。程式：`Equipment.swift`（七個部位、單手／雙手、`Gears.all` 資料表 24 款）、`Materials.scraps`（所有魔獸都可能掉的碎布／木片／廢鐵）、`Ant.gear`（出手、血量、擋擊、速度、射程都算裝備）、`Colony.craft`（扣材料、交給最需要的哥布林）、`WorkshopWindow.swift`（手動排版的工坊視窗）、`AntView.drawGear` 與 `drawSlash`（畫裝備、揮砍、長槍前刺、弓箭、法杖光球）。
- 已驗證：24 款配方都做過一遍並交給不同的哥布林；工坊視窗（材料夠是綠色、不夠是紅色、按鈕停用）；畫出整排哥布林（四個面向）的裝備與攻擊到一半的樣子；存檔後重開裝備還在；雙手武器不算盾。
- 裝備歸還（使用者問「老死應該會送回巢穴吧」）：哥布林老死或戰死時，裝備立刻進巢穴庫存（`Colony.armory`），馬上交給最需要的哥布林（`redistributeArmory`）；新生的哥布林會領庫存的舊裝備；換下來的舊裝備也進庫存；庫存存檔。已驗證：讓哥布林 4 倍速老死 215 隻，71 件裝備一件都沒少（穿著的＋庫存的＝做出的）。
- 已知：裝備平衡（出手與血量的數字）是初值；沒有把裝備顯示在名冊表格的欄位（只在點選後的詳細資料）。
- 順帶：`CAMP_TEST_WORKSHOP`、`CAMP_TEST_GEARSHEET` 兩個測試用環境變數。

### 選中哥布林（或點名冊）畫面超卡（使用者回報，已修）

原因：選取圈用虛線畫（`drawSelectionRing`），虛線的相位用「距 2001 年的秒數 × 12」，是上百億的數字；CoreGraphics 畫虛線時要從頭走完這個相位，一個畫面要花好幾秒（取樣看到全部時間都在 `path_iterator_dasher_create`）。這是很久以前就有的問題，隨時間越來越糟。改成相位對 7（虛線一圈的長度）取餘數。已驗證：選了一隻後每個畫面 13 毫秒，跟沒選一樣（之前選了就卡住）。新增 `CAMP_TEST_PERF` 量畫面時間。

### 裝備耐久度（使用者：「不然永遠都在好像也滿怪的」）

每件裝備有耐久（`Gear.durability`、`GearItem.left`），武器每出手磨損、盾牌與防具被打到磨損，剩不到 25% 效果減半，用完就壞並回收三成材料；歸還庫存的舊裝備保留剩下的耐久；存檔會存耐久（舊存檔只有裝備 id 的也讀得進來）。規則與數字在 [EQUIPMENT.md](EQUIPMENT.md)。已驗證：4 倍速老死加魔獸常來時，「穿著＋庫存＋壞掉＝做出的」一直相等（67 件）；存檔裡有耐久數字。沒做：修理。順便：浮字（製作、歸還、壞了、+素材）同時最多 3 個並錯開排列，避免大量死亡或搬運時洗版（使用者的截圖是我用 3 倍速壽命測試造成的，正常一隻哥布林活一天）。

## v0.5.0（2026-09-20）

- 版本 0.4.0 → **0.5.0**（build 5）。這一版新增：**魔獸來襲**（史萊姆、巨鼠，有攻擊動作、各自血量、被打時停下反擊、掉落素材、公主躲藏與治療）、**倉庫與魔獸圖鑑**、**工坊與裝備**（七個部位 24 款、穿在身上、哥布林攻擊動作、裝備歸還庫存、耐久度與損壞回收）、雜物（碎布、木片、廢鐵），並修好選中哥布林或點名冊會卡住的問題（選取圈虛線相位過大）與開場抬公主時縮放視窗會讓抬的哥布林卡住的問題。設計與規劃見 [MONSTERS.md](MONSTERS.md)、[EQUIPMENT.md](EQUIPMENT.md)。
- 給同事更新：`./build.sh --release` 會產生 `dist/GoblinCamp-0.5.0.zip`；存檔與設定都保留（存檔新增的欄位舊版讀不到但不影響）。

## 哥布林的日常（第一批，尚未 commit）

使用者希望哥布林平常不只是閒晃：釣魚、睡覺、煮飯、耕田、打架、照顧小哥布林、玩；並說每種哥布林可能不同：聰明的不會跟其他哥布林打架，金皮應該會奴役普通哥布林。決定：第一批做睡覺、釣魚、玩耍、看書、打鬧，加兩個品種個性；第二批（煮飯、耕田、照顧小哥布林）之後。使用者補充：睡覺不要 zzz，因為睡覺很偶而才翻身比較不耗效能；如果突然影響效能，應該減少哥布林出現或做別的措施。金皮「奴役」做成「使喚」（普通哥布林在旁伺候），沒有暴力。

- 設計與數字見 [LIFE.md](LIFE.md)。程式：`Ant.Activity`（睡覺、釣魚、玩、看書、打鬧、巡視、伺候）、`Ant.pickActivity`（依品種與是不是晚上挑）、`Colony.matchmake`（配打鬧的一對與金皮的隨從，每 2.5 秒一次）、`Personality`（品種個性）、`Pond.fishingSpots`、`AntView.drawActivity`、`Perf.swift`（`PerfGovernor` 效能保護）。
- 已驗證：白天與各品種的活動統計（聰明的一次都沒有打鬧、金皮巡視有隨從、其他品種各有偏好）；釣魚（釣竿、浮標、漣漪）、看書、側躺睡覺、金冠、打鬧灰塵的畫面；效能保護把 150 隻降到 64 隻同時出現；一天一個畫面約 1～2 毫秒（150 隻）。
- 測試中找到並修好兩個真的會卡住的地方：①釣完魚回到閒晃時站在離水太近的岸邊，任何一步都被判定「離水太近」而原地不動，改成岸邊的哥布林只要不走進水裡就能移動；②伺候的哥布林站位可能超出範圍（金皮在邊緣時），之後在範圍外走不回來，改成伺候的站位限制在範圍內，且在範圍外的哥布林會自己走回來。
- 沒驗證：夜晚（睡覺很多）時長時間的樣子；真實金皮很少（1%），巡視很少見；煮飯、耕田、照顧小哥布林還沒做。

## 營地視窗的地貌（使用者要更多樣的場景，尚未 commit）

使用者：池塘雖然有變化，但一直在同個地方、大小差不多，希望真的建立更多元的場景，隨機或持續改變。我們談定三步：①隨機生成、種類更多（先做，含四種主題）②持續改變（季節、水窪、樹苗長大、踩出路）③與玩法連起來（挖石頭、砍樹、沼澤走得慢、耕田、各地貌的魔獸）。使用者決定先做第一步；主題先做草地、森林空地、雪地、沼澤；只有按「重新生成地貌」才換新的（每個營地保持自己的樣子）。

做完第一版後使用者說「看起來超醜」，並要求「再做好看一點，哥布林還是要住在有點原始一點的地方」。截圖是我測試用的沼澤：地面平、爛泥像半透明的白橢圓、枯樹又細又乾、小路是一條直線、沒有人住的感覺。改成：地面加一大片一大片的深淺色塊；爛泥改成深色帶水光；蜿蜒且越遠越窄的小路；垂柳、香蒲；以及**原始的營地**（踩實的泥地、獸皮帳篷、圖騰柱、曬肉架、柴堆、骨頭、頭骨木樁、長矛、石頭營火坑）；還有立石、蕨類、樹樁等。

- 設計與內容見 [TERRAIN.md](TERRAIN.md)。程式：`Terrain.swift`（生成、烘成一張圖、障礙 `Solid`）、`Pond.swift`（三種水）、`tools/make_terrain.py`（約 100 張像素圖）、`Colony.scene`（障礙與營火坑）、`AntView.drawMapBackground`（畫烘好的圖、只重畫池塘閃光）、選單「營地視窗的地貌」、`Settings.terrainSeed/terrainBiome`。`AntWorld.obstacles` 改成通用的 `Obstacle`（池塘、樹幹、石頭、溪），`AntWorld.ponds` 給釣魚。
- 已驗證：四種主題各四個種子的總表；森林與沼澤的營地視窗畫面（帳篷、營火坑、圖騰、垂柳、立石、溪與橋）；150 隻哥布林時每個畫面 1～2 毫秒；卡住偵測 0～1 隻。
- 沒驗證：把視窗縮放拉大拉小時場景變動是否順（每 12 點的變化會重新生成，重新烘圖）；小視窗；夜晚色調下的雪地與沼澤；真正的溪把營地與其他區域隔開時哥布林的分布（橋很少人走，先看看）；季節等持續改變都還沒做。
- 也順便加入：`CAMP_TERRAIN`、`CAMP_TERRAIN_SEED`、`CAMP_TEST_TERRAINSHEET`（取代 `CAMP_POND_SEED`）。

### 營地慢慢長大（使用者：「營地是慢慢成長的，一開始絕對沒有那麼完整喔！」）

上一版把整套原始營地一次全放出來。改成依「營地曾經有過的最多哥布林數」（`Colony.peakAnts`，存檔的 `peak`）逐步出現：0 隻只有一小塊泥地，5 隻火坑，8～16 隻骨頭、樹樁、柴堆，22 隻第一頂帳篷，48 隻曬肉架，85 隻圖騰柱……泥地隨之擴大（`TerrainScene.growth`、`unlock`、`stage`；烘好的圖在階段改變時重畫，障礙也跟著增加）。時間表在 [TERRAIN.md](TERRAIN.md)。已驗證：同一個場景在 0、20、50、120 隻時的四張圖（森林與沼澤）。

使用者接著說：希望同事玩起來覺得**整個場地一整天甚至到明天後天都會持續改變，而且有點機率性、有可能不同**。這就是先前說好的第 2 步（持續改變），還沒做。

### 場地持續變化（使用者：「希望同事玩到會覺得整個場地一整天甚至到明天後天都會持續改變，而且有點機率性、都有可能不同」；「一定要保持美觀但是要有很多不同的可能性」，尚未 commit）

照先前提的順序做：下雨水窪 → 樹苗長大 → 踩出小路 → 季節（我建議季節加速，使用者同意）：一季 3 天，一年 12 天，每個營地起點不同。另外加了霧、螢火蟲、落葉、花瓣、雪等空氣中的小東西（讓不同時間看起來不同）。

- 設計見 [TERRAIN.md](TERRAIN.md)「場地會持續變化」。程式：`TerrainLife.swift`（`TerrainClock` 時間、`TerrainLifeState` 存檔內容、每 10 分鐘擲一次的事件、水窪、樹苗、被踩的地）、`Terrain.swift`（依季節位置 0～4 渲染：地面色調、積雪、換樹的樣子、落葉堆、水窪、被踩的地、長出來的樹、結冰的池塘與小溪）、`AntView.drawAtmosphere`（霧、螢火蟲、落葉、花瓣、雪、冬天下雪）、`Pond.image(frozen:)`（同一個池塘結冰的樣子）、`Colony.updateTerrainLife`（每 0.5 秒記哥布林站的位置、下雨時冒水窪、每 30 秒補算事件）、`tools/make_terrain.py`（嫩芽、小樹、少年樹的像素圖）。
- 美觀與多樣：所有變化都是像素畫；樹的樣子用「色彩混合」從同一張圖變出紅葉、光禿、花苞、積雪，不用多畫；十二個不同場景（四種主題各三個種子）在同一天的總表看起來都很不同（春天開花、秋天紅葉、雪地、沼澤…）。
- 已驗證：一年的總表（森林與草地）、十二個場景的總表、營地視窗用 1500 倍時間跑的實際畫面（秋天的樹、落葉、水窪、被踩的地、樹苗）、存檔裡有 `terrain`（樹苗、水窪、被踩的格子）。
- 沒驗證：真實時間（不加速）跑一整天的樣子；關掉程式一夜再打開的補算；冬天實際下雪時哥布林的畫面；夜晚的螢火蟲（只看過程式，沒看過實際畫面）；視窗拖曳縮放時樹苗與水窪的位置（用比例，會跟著移動）。
- 順便：新增 `CAMP_TERRAIN_SPEED`、`CAMP_SEASON_DAYS`、`CAMP_TEST_SEASONS`。

## v0.6.0（2026-09-21）

- 版本 0.5.0 → **0.6.0**（build 6）。這一版新增：**哥布林的日常**（睡覺、釣魚、玩耍、看書、打鬧；聰明的不打鬧，金皮巡視並有人伺候）、**效能保護**（太卡時減少同時出現的哥布林）、**營地視窗的地貌**（草地、森林空地、雪地、沼澤四種主題，由種子生成，溪與橋、樹叢、石頭、立石、池塘各不相同）、**原始的營地**（帳篷、圖騰柱、曬肉架、石頭營火坑，隨營地長大逐步出現）、**場地持續變化**（一季 3 天的四季、雨後水窪、樹苗長大、被踩出的小路、霧、螢火蟲、落葉、雪，都有機率性，會補算離線時間）。設計見 [LIFE.md](LIFE.md)、[TERRAIN.md](TERRAIN.md)。
- 給同事更新：`./build.sh --release` 會產生 `dist/GoblinCamp-0.6.0.zip`；存檔與設定都保留（存檔新增了 `peak`、`terrain` 等欄位，舊版讀不到但不影響）。第一次開新版時，營地視窗會產生自己的場景與季節起點。
- 同事玩過之後要看的：一季 3 天是不是太快或太慢（`CAMP_SEASON_DAYS` 可以試）、場地變化的速度、效能保護有沒有在不該啟動時啟動（選單「複製診斷資訊」會列出目前比例）。

### 砍樹與採石（使用者同意先做「讓哥布林參與場地變化」，並要求「都要記得加入機率變數」；接著問「樹會不會都被砍光？」；尚未 commit）

哥布林新增兩個活動：砍樹（木片）、採石（廢鐵），是工坊材料的另一個來源。機率變數放在：挑哪個活動與哪個目標（依品種）、需要敲幾下（力氣大的少）、結果（整棵倒下／只有樹枝／太硬；敲下鐵／碎石／一大塊）、稀有發現（5% 蜂巢、4% 碎晶）、2% 受傷；場地的變化（樹樁腐爛要 1.5～5 天、石頭是否縮小、被挖光的石頭何時浮出新的）。碎晶用來做新裝備「晶刃」。

- **不會砍光**：站著的樹只剩 6 棵不再倒；剩下越少機率越低；樹苗持續補上。被挖光的石頭平均約 1 週後有新的浮出來（`TerrainLife.advance`），鐵不會永遠斷。
- 程式：`Ant.Activity.gather`、`Ant.pickActivity`、`AntWorld.resources`、`TerrainScene.resourceSpots/cut/afterCut`（倒下的樹留樹樁、石頭縮小或消失，障礙也跟著變）、`TerrainLifeState.cuts`（存檔，欄位用手寫的解碼，舊存檔也讀得進來）、`Colony.finishGathering`／`dropLoot`、`AntView.drawActivity`（斧頭、鎬子的揮動與木屑火花）、`Materials.finds`、`Equipment` 的晶刃。
- 已驗證：加快 25 倍時，哥布林確實去砍樹採石，木片與鐵進了倉庫，樹從 10 棵減到 7 並出現樹樁；畫面上有斧頭與木屑。
- 沒驗證：不加速時的頻率（預計約每小時幾次）；調整視窗大小後被砍的樹是否會「復原」（場景重新生成，位置對不上）；碎晶與蜂巢的實際出現（機率低，測試時沒遇到）。

### 蘑菇不要圍成一圈（使用者：「應該是有隨機性之類的」）

原本森林的蘑菇是固定的「蘑菇圈」。改成一叢一叢隨機散布：每叢 1～6 朵，三種大小、四種顏色（紅底白點、棕、淺黃、紫），一叢大多同色但會混入別色；六成五長在樹、樹樁、倒木、石頭旁邊，其餘在空地；草地與沼澤也有少量；只有約八分之一的場景有一個蘑菇圈；秋天最多、冬天沒有（`TerrainScene.scatterMushrooms`）。

### 第二批日常活動：煮飯、耕田、照顧小哥布林（尚未 commit）

使用者要求每一項都要有機率變數。做法與數字都寫在 [LIFE.md](LIFE.md)「第二批」。

- 程式：`Colony.larder`／`finishCooking`（食材櫃、煮菜、結果的機率）、`FoodKind.stew`、`Ant.Activity.cook/farm/mind`、`PlotState` 與 `TerrainLife.advance` 的作物成長、`TerrainScene.plotSpots/plotInfos/drawFarm`（田與作物的畫法）、`Colony.finishFarming`、`Ant.isChild/sick`、`Colony.updateChildren/tend`、`AntView.drawActivity`（鍋、火、蒸氣、勺子、鋤頭、種子、籃子、澆水壺）與小哥布林的畫法（小一號、生病的綠色、愛心）、`tools/make_terrain.py`（田與三種作物各三個階段）。
- 已驗證：燉菜真的煮出來、食材櫃被用掉、魚一直補進去；田從翻土、播種、澆水走到長大、熟了有人收成；小哥布林有人照顧、頭上愛心、生病的樣子；沒有新的卡住。

### 使用者的回饋：魔獸太快出現、一開始太多隻，「你要判斷遊戲時間」（同一段）

**根本原因**：第一波魔獸的計時器初始值讓它在有 8 隻哥布林時立刻出現（大約遊戲開始 3 秒）。我的測試又把生成速度調成每 0.2 秒一隻，所以一下 150 隻，而且全是剛出生的小哥布林，小哥布林看到魔獸都跑回洞裡，巢穴洞口全卡滿（使用者回報「營地洞口卡著了」）。

- 魔獸改成依**遊戲時間**與營地規模：史萊姆遊戲 30 分鐘後、營地曾有 25 隻以上；巨鼠 75 分鐘後、曾有 40 隻以上；第一次還要再隨機等一段時間。動物 6 分鐘後才來，砍樹採石要 12 隻以上。遊戲時間 `Colony.playSeconds` 存檔。
- **進度跟著生成速度**（使用者：「不管生成速度或成長速度，都要因為哥布林的生成速度快慢有所不同；天氣季節不用」）：`Settings.pace`（每 3 分鐘一隻是 1 倍，範圍 0.25～4 倍）。跟著走的：遊戲時間（魔獸、動物、果樹的節奏）、小哥布林長大、作物與樹苗生長與冒出的機率、樹樁腐爛、石頭再浮出來。不跟著走的：季節、天氣、水窪、日常活動的頻率。
- 已驗證：生成速度 180／45／720 秒時，同樣 33 秒的遊戲時間分別是 37／151／9 秒（倍率 1／4／0.25）；一般速度下 90 秒內沒有魔獸；把時間加快後魔獸才出現；卡住偵測沒有問題。
- 測試小提醒：用很小的 `CAMP_SPAWN_INTERVAL` 測試時遊戲會以 4 倍走，需要固定節奏時加 `CAMP_PACE=1`。

### B 清單（使用者：「B 優先處理，然後順便幫我整理一下選單」「看是否有些需要整合整理重新命名，看起來更專業」「圖鑑等等」；尚未 commit）

- **B1 縮放視窗讓場景重新生成、被砍的樹復原（已修）**：場景改成固定大小（2200×1500）、以巢穴為中心的大畫布，營地視窗只是取一塊來看；縮放視窗只多看或少看，不重新生成（`TerrainScene.canvas/world`）。變過的東西（砍倒的樹、樹苗、水窪、被踩的地）改用「離巢穴的點數」記錄（舊存檔的位置沒有意義，第一次載入會重來，`layout` 欄位）。順便修好一個會 crash 的 bug：畫布座標有負數時，畫溪與小路會索引超出範圍。已驗證：縮放視窗時樹與石頭的數量隨視窗變、巢穴不動時場景不變。
- **B2 更多魔獸**：新增沼澤青蛙、蝙蝠（見 [MONSTERS.md](MONSTERS.md)），資料檔新增 `biomes/nightOnly/needsWater/flies`；地貌與夜晚會影響誰來。三種新素材組、三件新裝備。已驗證：兩種都被哥布林打倒。沒做：蘑菇怪、石甲蟲（要新的攻擊風格）。
- **B3 裝備修理**：工坊有「修理」區（見 [EQUIPMENT.md](EQUIPMENT.md)）。已驗證：修好一件雙手劍，材料扣掉、耐久回到 100%。遺物沒做（死去的哥布林的裝備本來就回庫存，可視為遺物）。
- **B4 名冊欄位**：新增「狀態」「裝備」兩欄，拿掉不常用的「速度／感知／搬運」（詳細資料裡還有）。
- **B5 季節與營地**：冬天雪人與第二堆柴火、秋天乾草與南瓜（見 [TERRAIN.md](TERRAIN.md)）。沼澤走得慢、夏天涼棚沒做。
- **選單整理**：原本 40 多個平鋪的項目，改成：最上面常用的（暫停、模式、番茄鐘、Claude Code），底下分「營地」「名冊與圖鑑」「顯示」「偏好設定」四組；命名改得一致（例如「倉庫與魔獸圖鑑」→「魔獸與素材圖鑑」放進「名冊與圖鑑」，「走動範圍與背景」→「走動範圍與地圖」，「效能保護」「天氣」「地貌」等進到對應的群組；營地視窗的相關設定收進「營地視窗」子選單；舊的「蟻窩」字樣改成「營地」）。README 有新的「選單一覽」與「功能與設定說明」，說明手冊與各份文件的選單路徑都改了。
- **找到並修好一個效能退步**：進度跟著生成速度（`Settings.pace`）與小孩長大（`Ant.childSeconds`）每次都去讀環境變數與設定，每個畫面被呼叫成千上萬次，讓每個畫面從約 1 毫秒變成 20 毫秒，會讓「效能保護」誤以為太卡而把哥布林藏起來。改成快取後回到約 1 毫秒。
